#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), *%w[.. lib])
require 'pwm/dialog'

begin
  puts Pwm::Dialog::MacOSDialog.new.get_password
rescue Pwm::Dialog::Cancelled
  exit $!.message
end
